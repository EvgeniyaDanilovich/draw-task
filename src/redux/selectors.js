export const selectDesks = (state) => state.desks.desks;
export const selectCurrentDesk = (state) => state.desks.currentDesk;
export const selectUsername = (state) => state.desks.username;
export const selectSocket = (state) => state.desks.socket;